import 'package:flutter/material.dart';

class AffectationPersonnelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Affectation Personnel'),
      ),
      body: Center(
        child: Text('Page d\'Affectation du Personnel'),
      ),
    );
  }
}